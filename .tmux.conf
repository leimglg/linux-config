# -- general ------------------------------------------------------------------
bind r source-file ~/.tmux.conf \; display '~/.tmux.conf sourced'
set -g prefix C-x 
unbind C-b
set -g escape-time 10
set -g mouse on
set -g default-terminal "screen-256color"
setw -g xterm-keys on
set -sg repeat-time 600
set -s focus-events on
set -q -g status-utf8 on
setw -q -g utf8 on
set -g history-limit 5000

# -- display -------------------------------------------------------------------
set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows
setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed
set -g set-titles on          # set terminal title
set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time
set -g status-interval 10     # redraw status line every 10 seconds

set -wg window-status-separator " " # 状态栏窗口名称之间的间隔
# set -g status-left "#[bg=#FF661D] session #S "
set -g monitor-activity on
set -g visual-activity off

bind K split-window -v
bind L split-window -h

bind -r j select-pane -L  # move left
bind -r k select-pane -D  # move down
bind -r i select-pane -U  # move up
bind -r l select-pane -R  # move right
bind C-l swap-pane -D       # swap current pane with the next one
bind C-j  swap-pane -U       # swap current pane with the previous one

bind + run 'cut -c3- ~/.tmux.conf | sh -s _maximize_pane "#{session_name}" #D'

